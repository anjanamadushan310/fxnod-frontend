# ============================================================================
# Local development overrides. Copy to `.env.local` (gitignored) for `npm run
# dev`. Points at the local docker-compose stack (NGINX gateway on :80).
# ============================================================================
NEXT_PUBLIC_API_URL=http://localhost
NEXT_PUBLIC_WS_URL=ws://localhost
